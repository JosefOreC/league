# Renombra este archivo como .env.local para que Vite lo detecte y funcione en tu prueba local.
# No compartas tus archivos .env ni los subas a GitHub real.

# Dirección del Backend en Django (por defecto localhost:8000)
VITE_API_URL=http://localhost:8000/api
