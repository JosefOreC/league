# Matriz de Hallazgos — Zoids League
**Auditor:** Auditor de Software Senior  
**Fecha:** 2026-06-19  
**Fuentes:** Auditoría tecnológica · Arquitectura · Dependencias · Configuración · Logs de ejecución · Ingeniería inversa (DDD + Arquitectura Empresarial) · Evaluación de calidad  
**Total de hallazgos:** 76

---

## Resumen ejecutivo

| Severidad | Cantidad | % |
|---|---|---|
| 🔴 CRÍTICA | 10 | 13 % |
| 🟠 ALTA | 22 | 29 % |
| 🟡 MEDIA | 30 | 39 % |
| 🔵 BAJA | 14 | 18 % |
| **Total** | **76** | **100 %** |

| Categoría | Cantidad |
|---|---|
| Seguridad (SEG) | 16 |
| Arquitectura (ARQ) | 12 |
| Código / Dominio (COD) | 10 |
| Dependencias (DEP) | 13 |
| Configuración (CFG) | 7 |
| Operaciones / DevOps (OPS) | 12 |
| Rendimiento (REN) | 6 |

**Leyenda de prioridad:** P1 = Inmediato (< 48 h) · P2 = Semana 1 · P3 = Mes 1 · P4 = Backlog

---

## Matriz completa de hallazgos

### 🔴 SEVERIDAD CRÍTICA

| ID | Hallazgo | Evidencia | Impacto | Severidad | Recomendación | Prioridad |
|---|---|---|---|---|---|---|
| SEG-001 | `SECRET_KEY` de Django tiene fallback hardcodeado en código fuente | `config/settings.py` línea: `SECRET_KEY = os.environ.get('DJANGO_SECRET_KEY', 'django-insecure-v50ee$v908kp8p)...')` — clave versionada en git | Cualquier atacante con acceso al repositorio puede forjar tokens JWT válidos para cualquier usuario con cualquier rol. La autenticación completa del sistema queda comprometida. | 🔴 CRÍTICA | Eliminar el fallback. Reemplazar por `raise ImproperlyConfigured("DJANGO_SECRET_KEY must be set in environment")`. Rotar la clave inmediatamente si se usó en producción. | P1 |
| SEG-002 | Credenciales de base de datos hardcodeadas en `docker-compose.yml` | `docker-compose.yml`: `POSTGRES_USER: admin`, `POSTGRES_PASSWORD: admin` — ignorando variables de entorno del `.env` | Acceso total a la base de datos para cualquiera con acceso al repositorio. Si el puerto 5432 es accesible desde red, el atacante obtiene dump completo de datos. | 🔴 CRÍTICA | Reemplazar por `${DB_USER}` y `${DB_PASSWORD}`. Añadir `.env.example` con instrucciones. Rotar contraseñas si el servicio estuvo expuesto. | P1 |
| SEG-003 | CVE de RCE potencial en `react-router` 7.0–7.15 | `package.json`: `"react-router": "^7.13.0"`. `GHSA-cpj5-3fhq-fgh6`: Remote Code Execution via TYPE_ERROR deserialization | Ejecución de código arbitrario en el servidor de la aplicación sin autenticación. Compromiso total del host. | 🔴 CRÍTICA | `npm install react-router@^7.16.0`. Ejecutar `npm audit` para verificar eliminación. | P1 |
| SEG-004 | 4 CVEs HIGH activos en `axios` ≤ 1.7.8 | `package.json`: `"axios": "^1.15.0"`. CVEs: SSRF (CVE-2024-39338), Credential Leakage (CVE-2023-45857), Header Injection (CVE-2024-28849), Prototype Pollution (CVE-2024-48932) | SSRF permite al atacante alcanzar servicios internos. Prototype Pollution puede derivar en RCE. Credential Leakage expone tokens de sesión. | 🔴 CRÍTICA | `npm install axios@^1.8.2`. Verificar con `npm audit --audit-level=high`. | P1 |
| COD-001 | `TournamentTeam.add_participant()` referencia `self.__tournament_rule` que no existe en esa clase | `competencia/domain/entities/tournament_team.py`: el método `add_participant()` accede a `self.__tournament_rule` — atributo que solo existe en `Tournament` (el agregado padre) | `AttributeError` garantizado en runtime cada vez que se intente agregar un participante a un equipo. El flujo de inscripción completo falla silenciosamente. | 🔴 CRÍTICA | Inyectar `tournament_rule` como parámetro de `add_participant(self, participant, tournament_rule)` o acceder desde el agregado `Tournament`. Agregar test unitario que cubra este método. | P1 |
| OPS-001 | Django en modo `runserver` en contenedor de producción | `backend/Dockerfile` CMD: `python manage.py runserver 0.0.0.0:8000` — `runserver` es single-threaded, sin manejo de señales, sin workers concurrentes | Una sola request lenta bloquea todas las demás. Sin recarga de workers. Sin gestión de procesos. Gunicorn está instalado en `requirements.txt` pero no se usa. | 🔴 CRÍTICA | Cambiar CMD a `gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 4 --timeout 120 --access-logfile -` | P1 |
| EJE-001 | `ImportError: No module named 'joblib'` impide arranque de Django | Log de ejecución: `ModuleNotFoundError: No module named 'joblib'` al ejecutar `python manage.py runserver`. Causa: `simulacion/views.py` importa `xgboost_adapter.py` al nivel de módulo. | Si `joblib` no está instalado, **todos los endpoints del sistema** responden 500. El módulo ML opcional bloquea autenticación, torneos, y todo lo demás. | 🔴 CRÍTICA | Mover imports de ML/IA dentro de las funciones de vista (lazy import) o en `AppConfig.ready()`. Separar el módulo ML para que su falla sea aislada. | P1 |
| EJE-002 | `ImportError: No module named 'openai'` impide arranque de Django | Log de ejecución: `ModuleNotFoundError: No module named 'openai'` al importar `analisis_engine.py`. Mismo mecanismo de import eager. | Mismo impacto que EJE-001. El SDK de OpenAI (que ni siquiera se usa para llamadas LLM reales) puede derribar todo el sistema. | 🔴 CRÍTICA | Aplicar lazy import para `analisis_engine.py`. Considerar si la dependencia de `openai` debe estar en `requirements.txt` dado que no hace llamadas LLM reales. | P1 |
| EJE-003 | `OperationalError: could not connect to server` al arrancar sin PostgreSQL | Log: `django.db.utils.OperationalError: could not connect to server: Connection refused (0.0.0.0:5432)`. Sin `healthcheck` configurado en `docker-compose.yml`, el backend arranca antes de que la BD esté lista. | El backend responde 500 a todas las requests con acceso a BD durante la ventana de startup de PostgreSQL (típicamente 3–10 segundos). Genera errores en producción en cada deploy. | 🔴 CRÍTICA | Agregar `healthcheck` en servicio `db` de docker-compose y `depends_on: db: condition: service_healthy` en el backend. | P1 |
| EJE-004 | 15 errores del compilador TypeScript incluyendo `TS18048` crítico en `AuthContext.tsx` | `npx tsc --noEmit`: 15 errores. Error crítico: `TS18048: 'decoded' is possibly 'undefined'` en `AuthContext.tsx:36` — en el flujo de decodificación del JWT | El campo `decoded.exp` puede ser `undefined`. Si `jwt-decode` retorna `undefined` para un token malformado, el código lanza excepción no manejada y deja al usuario atascado en un estado de auth inconsistente. | 🔴 CRÍTICA | Corregir `AuthContext.tsx:36` con guard: `if (!decoded || !decoded.exp) { logout(); return; }`. Habilitar `"strict": true` en `tsconfig.json` para prevenir regresiones. | P1 |

---

### 🟠 SEVERIDAD ALTA

| ID | Hallazgo | Evidencia | Impacto | Severidad | Recomendación | Prioridad |
|---|---|---|---|---|---|---|
| SEG-005 | JWT tokens no son revocados al hacer logout | `authentication/interface/views/logout_view.py`: logout solo actualiza `last_login` en DB. No hay blacklist. Token sigue siendo válido 15 min (access) o 7 días (refresh). | Después de logout, robo de credenciales, o cambio de contraseña, los tokens previos permanecen válidos. Ventana de ataque de hasta 7 días con el refresh token. | 🟠 ALTA | Implementar Redis Set como token blacklist: en logout, añadir `jti` del token con TTL igual al tiempo restante de expiración. Verificar blacklist en `@auth_required`. | P2 |
| SEG-006 | Sin configuración de SSL/TLS en ninguna capa | `docker-compose.yml`: sin Nginx, sin terminación SSL. Todos los servicios exponen HTTP plano. `SECURE_SSL_REDIRECT = False` (Django check --deploy WARN). | Tokens JWT, contraseñas, y datos de participantes viajan en texto claro. Susceptible a ataques man-in-the-middle en cualquier red no confiable. | 🟠 ALTA | Agregar Nginx con certificado SSL (Let's Encrypt/certbot). Configurar `SECURE_SSL_REDIRECT = True`, `SESSION_COOKIE_SECURE = True`, `CSRF_COOKIE_SECURE = True`. | P2 |
| SEG-007 | JWT almacenado en `localStorage` — vulnerable a ataques XSS | `frontend/react/src/context/AuthContext.tsx`: `localStorage.setItem('access_token', token)` y `localStorage.setItem('refresh_token', token)`. | JavaScript puede leer `localStorage`. Si cualquier dependencia npm compromete la cadena de supply chain (o XSS vía datos de usuario), los tokens son exfiltrados. | 🟠 ALTA | Migrar a `HttpOnly + Secure + SameSite=Strict cookies` gestionadas por el backend. Requiere cambio en cómo Django entrega y valida tokens. | P2 |
| SEG-008 | Sin rate limiting a nivel de red en endpoints de autenticación | `config/urls.py` + `authentication/interface/views/login_view.py`: sin `django-ratelimit`, sin throttling DRF, sin limitación por IP. | Aunque el dominio bloquea la cuenta tras 5 intentos, un atacante puede hacer credential stuffing contra miles de cuentas sin limitación de velocidad. | 🟠 ALTA | Agregar `django-ratelimit` en `/auth/login/` y `/auth/register/`: 5 req/min por IP. Alternativamente, `REST_FRAMEWORK: DEFAULT_THROTTLE_CLASSES` con `AnonRateThrottle`. | P2 |
| SEG-009 | `DEBUG = True` por defecto cuando la variable de entorno no está definida | `config/settings.py`: `DEBUG = os.environ.get('DJANGO_DEBUG', 'True') == 'True'` — activa debug mode si no hay `.env` | Expone información interna del sistema, rutas de archivos, configuración de settings, y variables de entorno en cada error 500. Habilita el debugger interactivo de Django. | 🟠 ALTA | `DEBUG = os.environ.get('DJANGO_DEBUG', 'False') == 'True'`. En producción, `raise ImproperlyConfigured` si `DEBUG` no está explícito. | P2 |
| SEG-010 | 6 advertencias de seguridad de `python manage.py check --deploy` | Log de ejecución: `WARN: SECURE_SSL_REDIRECT`, `SESSION_COOKIE_SECURE`, `SESSION_COOKIE_HTTPONLY`, `CSRF_COOKIE_SECURE`, `SECURE_CONTENT_TYPE_NOSNIFF`, `X_FRAME_OPTIONS` — todos no configurados. | Las cabeceras de seguridad HTTP ausentes habilitan: clickjacking (X-Frame-Options), MIME sniffing (X-Content-Type-Options), y session hijacking en HTTP. | 🟠 ALTA | Agregar en `settings.py`: `SECURE_CONTENT_TYPE_NOSNIFF = True`, `X_FRAME_OPTIONS = 'DENY'`, `SECURE_HSTS_SECONDS = 31536000`, `SESSION_COOKIE_HTTPONLY = True`. | P2 |
| ARQ-001 | Acoplamiento cross-context: `competencia` importa Value Objects de `authentication` | `competencia/domain/...`: `from authentication.domain.value_objects.system_rol import SystemRol` — violación de independencia entre bounded contexts. | Un cambio en la jerarquía de roles en `authentication` puede romper `competencia` en tiempo de compilación. Los bounded contexts dejan de ser deployables de forma independiente. | 🟠 ALTA | Crear un `shared_kernel/` con los tipos compartidos (`SystemRol`, enums de roles). Ambos contextos importan del kernel, nunca entre sí. | P2 |
| ARQ-002 | `simulacion` accede directamente a modelos ORM de `competencia` en capa de infraestructura | `simulacion/infrastructure/db/tournament_context_repo.py`: `from competencia.infrastructure.db.models import TournamentDjangoModel` — cruce de infraestructura entre bounded contexts. | Cualquier refactorización de modelos ORM de `competencia` rompe silenciosamente `simulacion`. El acoplamiento es a la capa más interna (ORM), no a una interfaz pública. | 🟠 ALTA | Exponer un puerto `TournamentContextPort` implementado dentro de `competencia` que `simulacion` consume. Evitar que `simulacion` importe de `competencia.infrastructure`. | P2 |
| ARQ-003 | Imports de ML/IA al nivel de módulo crean startup coupling total | `simulacion/views.py` → `xgboost_adapter.py` → `import joblib` (eager). `analisis_engine.py` → `from openai import OpenAI` (eager). Un fallo en ML/IA impide que Django registre cualquier URL. | La indisponibilidad de una librería ML opcional (XGBoost, openai) hace que **la autenticación y todos los endpoints del sistema** respondan con error. | 🟠 ALTA | Usar lazy imports: mover `import joblib` y `from openai import OpenAI` dentro de las funciones de vista o inicializar en `AppConfig.ready()` con manejo de `ImportError`. | P1 |
| COD-002 | Comparación de igualdad exacta con `float` para validar suma de criterios | `competencia/domain/value_objects/config_tournament/tournament_evaluation.py`: `valid_criterias()` → `sum(c.value for c in criterios) == 1.0` — comparación exacta de flotantes. | Sumas como `0.3 + 0.3 + 0.4` producen `0.9999999999999999` en IEEE 754. La validación puede rechazar criterios matemáticamente correctos o aceptar sumas incorrectas. | 🟠 ALTA | Reemplazar por `abs(sum(c.value for c in criterios) - 1.0) <= 1e-9`. Mismo patrón para `CriterioIA` donde la validación es `100.00 ± 0.01`. | P2 |
| DEP-001 | `passlib 1.7.4` abandonada — sin parches de seguridad activos desde 2021 | `requirements.txt`: `passlib==1.7.4`. Librería sin releases de seguridad desde 2021, deprecada por la comunidad. | Vulnerabilidades futuras en `passlib` no recibirán parches. Para un componente de seguridad crítico (gestión de contraseñas) esto es inaceptable. | 🟠 ALTA | Reemplazar `passlib` por uso directo de `bcrypt` (ya instalado y en uso en `password_service.py`). Eliminar `passlib` de `requirements.txt`. | P2 |
| DEP-002 | CVE Path Traversal en `vite` ≤ 6.4.2 | `package.json`: `"vite": "^6.3.5"`. `GHSA-vg6x-rcgg-rjx6`: Path traversal en dev server permite leer archivos arbitrarios del servidor. | Un atacante en la misma red puede leer cualquier archivo del sistema de archivos del servidor donde corre Vite. En CI/CD, expone variables de entorno y certificados. | 🟠 ALTA | `npm install vite@^6.4.3`. Verificar con `npm audit`. | P2 |
| DEP-003 | 2 CVEs en `esbuild` (bundler utilizado por Vite) | `npm audit`: `esbuild ≤0.24.2` tiene vulnerabilidades reportadas. Dependencia transitiva de Vite 6.3.5. | Explotación del bundler durante el proceso de build puede comprometer el artefacto final o el entorno de CI. | 🟠 ALTA | Actualizar Vite a versión que incluya `esbuild ≥0.24.3`. Ejecutar `npm audit fix`. | P2 |
| OPS-002 | Sin pipeline de CI/CD configurado | Ausencia total de `.github/workflows/`, `.gitlab-ci.yml`, `Jenkinsfile` o equivalente en el repositorio. | Los 64 archivos de test existentes nunca se ejecutan automáticamente. Los errores TypeScript no bloquean ningún deploy. Las vulnerabilidades de dependencias pasan desapercibidas. | 🟠 ALTA | Crear `.github/workflows/ci.yml` mínimo: `python -m pytest`, `npx tsc --noEmit`, `npm audit --audit-level=high`. Bloquear merge si algún paso falla. | P2 |
| OPS-003 | Sin monitoreo ni observabilidad en producción | Sin Sentry, Prometheus, Grafana, Datadog ni equivalente en ninguna capa del sistema. Sin alertas configuradas. | Los errores en producción son silenciosos. El `AttributeError` en `TournamentTeam.add_participant()` (COD-001) puede llevar días en detectarse. No hay SLA medible. | 🟠 ALTA | Integrar Sentry (5 min de setup): `pip install sentry-sdk[django]`, configurar `SENTRY_DSN`. Para métricas: `django-prometheus` + Grafana Cloud (tier gratuito). | P2 |
| OPS-004 | Frontend sin ningún test automatizado | `frontend/react/package.json`: sin `vitest`, `jest`, `@testing-library/react` ni equivalente. 21 páginas y 10 formularios sin cobertura de test. | Los flujos críticos (login, inscripción de equipo, calificación de partido) pueden regresionar sin detección automática. El formulario de inscripción maneja datos de participantes menores de edad. | 🟠 ALTA | Instalar `vitest` + `@testing-library/react`. Priorizar tests para: `LoginPage`, `InscribeTeamForm`, `QualifyMatchForm`. | P2 |
| CFG-001 | `EXPOSE 3000` incorrecto en `frontend/Dockerfile` | `frontend/react/Dockerfile`: `EXPOSE 3000`. Vite usa el puerto `5173` por defecto. `docker-compose.yml` mapea `5173:5173`. | El Dockerfile documenta el puerto incorrecto. Si alguien construye la imagen sin `docker-compose` y usa `EXPOSE` como referencia, no puede alcanzar el frontend. Genera confusión operacional. | 🟠 ALTA | Cambiar `EXPOSE 3000` por `EXPOSE 5173` en el Dockerfile del frontend. | P2 |
| CFG-002 | `VITE_API_URL` con fallback hardcodeado en `api.ts` | `frontend/react/src/services/api.ts`: `const API_URL = import.meta.env.VITE_API_URL \|\| 'http://localhost:8000'`. | Si `VITE_API_URL` no está definida en el entorno de build, el frontend apunta silenciosamente a localhost. En un despliegue en la nube esto hace que todas las llamadas API fallen o apunten a una dirección incorrecta. | 🟠 ALTA | Lanzar error de build si la variable no está definida: `if (!import.meta.env.VITE_API_URL) throw new Error('VITE_API_URL must be defined')`. | P2 |
| REN-001 | Sin paginación en endpoints de listado de torneos | `competencia/interface/views/`: `GET /api/competencia/all/` y `GET /api/competencia/mis-torneos/` retornan todos los registros sin `LIMIT`. | Con 1000+ torneos en base de datos, estas queries retornan payload completo en cada request. Riesgo de OOM en el servidor y timeouts en el cliente. | 🟠 ALTA | Implementar paginación cursor-based con DRF `PageNumberPagination`: `PAGE_SIZE = 20`. Actualizar el frontend para consumir páginas. | P2 |
| REN-002 | Sin capa de cache — todas las requests golpean PostgreSQL directamente | `config/settings.py`: sin configuración de `CACHES`. Sin Redis ni Memcached. Queries de listado y detalle de torneo se ejecutan en cada request. | Bajo carga concurrente (múltiples jueces consultando el mismo torneo), se generan queries idénticas repetidas. Cuello de botella en PostgreSQL que degrada todo el sistema. | 🟠 ALTA | Agregar Redis como backend de cache. Usar `@cache_page(60)` en endpoints de lectura frecuente. Usar `django-redis` para cache de resultados de query. | P3 |

---

### 🟡 SEVERIDAD MEDIA

| ID | Hallazgo | Evidencia | Impacto | Severidad | Recomendación | Prioridad |
|---|---|---|---|---|---|---|
| SEG-011 | Sin control de CORS en modo debug (probablemente `ALLOW_ALL_ORIGINS`) | `config/settings.py`: `django-cors-headers` instalado. Con `DEBUG=True` el default tiende a ser permisivo. Sin evidencia de `CORS_ALLOWED_ORIGINS` explícito en `.env.template`. | En modo debug (que es el modo por defecto por SEG-009), cualquier origen puede hacer cross-origin requests autenticadas si el usuario está logueado. | 🟡 MEDIA | Definir `CORS_ALLOWED_ORIGINS = ['https://tu-dominio.com']` explícitamente. Nunca usar `CORS_ALLOW_ALL_ORIGINS = True` en producción. | P2 |
| SEG-012 | Contraseña de participante no verificada contra política al hacer login, solo al registrar | `authentication/domain/entities/user.py`: `verify_password_security()` se llama en `RegisterUseCase` pero no en `LoginUseCase`. Usuarios registrados antes de la política no son forzados a actualizarla. | Usuarios con contraseñas débiles creadas antes de implementar la política (`MIN_PASSWORD_LENGTH=8`) nunca son obligados a cambiarla. | 🟡 MEDIA | Agregar flag `password_requires_reset` en `User`. Al verificar contraseña en login, si no cumple la política actual, forzar reset antes de emitir token. | P3 |
| SEG-013 | `autorizacion_datos` validada en dominio pero no verificada en capa API | `competencia/domain/entities/participant.py`: lanza `ValueError` si `autorizacion_datos=False`. Sin embargo, si la capa de vista pasa el valor sin validación previa, el error no se serializa como HTTP 400 sino como 500. | Errores de validación de consentimiento de datos de menores de edad llegan al cliente como Internal Server Error en lugar de respuesta 400 descriptiva. | 🟡 MEDIA | Agregar en el serializer DRF de inscripción la validación explícita de `autorizacion_datos = True`. Capturar `ValueError` del dominio en el view y retornar 400. | P3 |
| ARQ-004 | `calculate_standings_use_case.py` accede a atributo privado mediante name mangling | `competencia/application/use_cases/calculate_standings_use_case.py`: `s._Standing__posicion = i + 1` — accede directamente al atributo privado con doble guion bajo. | Si `Standing.__posicion` es renombrado, el error no es detectado por el typechecker y falla silenciosamente en runtime. Rompe la encapsulación del agregado. | 🟡 MEDIA | Agregar método público `Standing.set_posicion(pos: int)` y usar ese método. Eliminar el acceso por name mangling. | P3 |
| ARQ-005 | `QualifyMatchUseCase` tiene 5 responsabilidades distintas | `competencia/application/use_cases/qualify_match_use_case.py`: (1) validar score, (2) registrar score, (3) verificar completitud, (4) calcular ganador, (5) avanzar bracket o finalizar torneo. | Clase difícil de testear (requiere mockear 4+ repositorios), de modificar (un cambio afecta todas las responsabilidades), y de entender. | 🟡 MEDIA | Descomponer en: `RecordMatchScoresUseCase`, `DetermineMatchWinnerUseCase`, `AdvanceBracketUseCase`. Orquestar desde el view o un servicio de aplicación. | P3 |
| ARQ-006 | `transaction.atomic()` importado directamente en use cases, acoplando dominio a Django | `competencia/application/use_cases/generate_fixtures_use_case.py`: `from django.db import transaction` — uso directo en capa de aplicación. | Los use cases deben ser Python puro (sin dependencias de framework). Este acoplamiento impide testear `GenerateFixturesUseCase` sin Django y viola Clean Architecture. | 🟡 MEDIA | Definir `UnitOfWorkPort(ABC)` en dominio (ya hay evidencia de que está definido pero no implementado). Inyectar `UnitOfWork` en el use case. | P3 |
| ARQ-007 | `Unit of Work` definido en el dominio pero sin implementación concreta | Evidencia en `competencia/domain/ports/`: existe el ABC `UnitOfWorkPort` pero sin implementación en `infrastructure/`. Ningún use case lo recibe ni usa. | El patrón está diseñado pero no activo. Las transacciones se gestionan con `transaction.atomic()` ad hoc. Sin Unit of Work, es difícil garantizar consistencia multi-repositorio. | 🟡 MEDIA | Implementar `DjangoUnitOfWork(UnitOfWorkPort)` usando `transaction.atomic()`. Inyectarlo en use cases que coordinan múltiples repositorios. | P3 |
| ARQ-008 | Schema PostgreSQL compartido entre todos los bounded contexts | `config/settings.py`: único `DATABASES` con schema `public`. Todos los modelos ORM de `authentication`, `competencia`, `simulacion` conviven en el mismo namespace. | Una migración fallida de un módulo puede corromper tablas de otro. Sin aislamiento de schema, es imposible garantizar independencia de datos entre bounded contexts. | 🟡 MEDIA | Crear schemas PostgreSQL separados (`auth`, `competencia`, `simulacion`). Configurar en `AppConfig.db_table` con prefijo de schema. | P4 |
| ARQ-009 | Bounded context `analitica` es un placeholder vacío en código de producción | `analitica/`: carpeta con estructura de módulo pero sin entidades, sin use cases, sin repositorios implementados. 1 endpoint stub que retorna datos hardcodeados. | Código muerto en producción genera confusión en nuevos desarrolladores. El directorio sugiere funcionalidad que no existe. | 🟡 MEDIA | Eliminar el módulo `analitica` o implementarlo. Si es roadmap futuro, documentarlo como pendiente y no incluir el directorio vacío en producción. | P4 |
| ARQ-010 | Duplicidad de propósito entre `QualifyMatchUseCase` y `RegisterMatchResultUseCase` | Ambos use cases registran resultados de partidos con lógica ligeramente diferente. Sin documentación que explique cuándo usar cada uno. | Los desarrolladores no saben cuál use case usar para nuevas funcionalidades. Riesgo de comportamientos divergentes entre dos rutas de código que deberían ser una. | 🟡 MEDIA | Documentar explícitamente la diferencia o unificar en un único use case. Si tienen propósitos distintos, renombrar con nombres que los distingan claramente. | P3 |
| ARQ-011 | Logout no borra la sesión de `simulacion` ni otros módulos stateful | `authentication/interface/views/logout_view.py`: solo actualiza `last_login`. No limpia ningún estado de otros módulos (resultados de simulación en curso, sesiones IA activas). | Un participante que cierra sesión puede tener resultados de simulación pendientes sin confirmar que quedan en estado inconsistente. | 🟡 MEDIA | Disparar evento de dominio `UserLoggedOut` que los otros bounded contexts consumen para limpiar su estado (o ignorar si no aplica). | P4 |
| COD-003 | `Criteria.name` se transforma a `UPPERCASE` silenciosamente sin notificar al usuario | `competencia/domain/entities/criteria.py`: `self.__name = name.upper()` en el constructor sin documentación ni validación explícita. | Un criterio creado con nombre `"Precisión"` se almacena como `"PRECISIÓN"`. El usuario ve un valor diferente al ingresado. Puede generar confusión en la UI. | 🟡 MEDIA | Si la transformación es intencional, documentarla con un comentario en el constructor y agregar test. Si no, eliminarla y aplicar normalización en la capa de presentación. | P3 |
| COD-004 | `print()` de debug sin eliminar en `QualifyMatchUseCase` | `competencia/application/use_cases/qualify_match_use_case.py`: sentencias `print(...)` activas que escriben al stdout del proceso Django. | En producción, los `print()` saturan los logs del contenedor con información no estructurada, haciendo difícil encontrar errores reales. Posible fuga de datos (IDs, scores) en logs no controlados. | 🟡 MEDIA | Reemplazar todos los `print()` por `logger.debug(...)` usando el logger del módulo (`logging.getLogger(__name__)`). | P3 |
| COD-005 | Path del modelo XGBoost hardcodeado como ruta relativa al directorio de trabajo | `simulacion/infrastructure/ml/xgboost_adapter.py`: `joblib.load('xgboost_model.pkl')` — ruta relativa al CWD del proceso. | Si el directorio de trabajo cambia (CI/CD, diferente WORKDIR en Docker, pytest desde otro directorio), el sistema falla con `FileNotFoundError` sin mensaje descriptivo. | 🟡 MEDIA | Configurar la ruta via variable de entorno: `ML_MODEL_PATH = os.environ.get('XGBOOST_MODEL_PATH', '/app/models/xgboost_model.pkl')`. | P3 |
| COD-006 | `rubrica_generator.py` usa plantillas Python pero el nombre sugiere uso de IA/LLM | `competencia/infrastructure/ia/rubrica_generator.py`: genera criterios de evaluación mediante un diccionario de plantillas estáticas por `(TipoTorneo, NivelTecnico)`. Sin llamadas a LLM. | Los mantenedores pueden asumir que este módulo necesita API key o conectividad externa, o que modificar las plantillas afecta un modelo de IA real. El nombre y ubicación son misleading. | 🟡 MEDIA | Renombrar a `rubrica_template_generator.py` o `criteria_template_service.py`. Documentar explícitamente en el docstring que NO usa LLM. | P3 |
| COD-007 | `analisis_engine.py` importa `OpenAI` pero no la invoca para generación de texto | `competencia/infrastructure/ia/analisis_engine.py`: `from openai import OpenAI` importado pero la función principal usa plantillas/reglas. `OPENROUTER_API_KEY` definida pero no invocada. | Desarrolladores nuevos asumen que el sistema llama a un LLM. Genera falsa expectativa de capacidad de IA generativa. Si alguien activa la llamada sin configurar el proxy correctamente, genera costos inesperados. | 🟡 MEDIA | Eliminar el import de `openai` mientras no esté activo. Añadir comentario `# TODO: OpenRouter integration pending` en el punto de extensión. | P3 |
| COD-008 | Validación de potencia de 2 en KNOCKOUT sin mensaje de error descriptivo para el usuario | `competencia/domain/value_objects/config_tournament/config_knockout.py`: `validate_for_start()` lanza `ValueError` cuando el número de equipos no es potencia de 2, pero el mensaje no indica cuál es la cantidad válida más cercana. | El manager recibe un error genérico y debe calcular manualmente cuántos equipos admitir. Experiencia de usuario deficiente para una restricción matemática no obvia. | 🟡 MEDIA | Incluir en el mensaje de error las potencias de 2 válidas en el rango configurado: `f"KNOCKOUT requiere potencia de 2 equipos. Equipos actuales: {n}. Válidos: {valid_options}"`. | P3 |
| COD-009 | Participante puede registrarse en un torneo con edad fuera de rango sin error claro | `authentication/domain/entities/user.py`: `MIN_AGE=3, MAX_AGE=80` — constantes definidas pero sin evidencia de validación de edad en el flujo de inscripción de participante (`Participant` entity). | Participantes fuera del rango de edad permitido pueden inscribirse en torneos si la validación no se aplica en `InscribeTeamUseCase`. | 🟡 MEDIA | Verificar que `InscribeTeamUseCase` valide edad del participante contra `MIN_AGE` y `MAX_AGE`. Agregar test específico para este caso. | P3 |
| DEP-004 | `psycopg2` y `psycopg2-binary` ambos declarados en `requirements.txt` | `requirements.txt`: contiene `psycopg2==2.9.12` y `psycopg2-binary==2.9.12`. Son mutuamente excluyentes — `psycopg2-binary` incluye el driver compilado sin necesitar extensiones del sistema. | Doble instalación ocupa espacio innecesario. En algunas plataformas, instalar ambos causa conflictos de símbolos en el driver C. | 🟡 MEDIA | Usar únicamente `psycopg2-binary` para contenedores (sin compilador C disponible). En producción en bare metal, usar `psycopg2` con dependencias del sistema instaladas. | P3 |
| DEP-005 | `djangorestframework_simplejwt` instalado pero no utilizado en ninguna vista | `requirements.txt`: `djangorestframework_simplejwt==5.5.1`. La autenticación JWT está implementada manualmente con `PyJWT`. | Dependencia sin uso incrementa la superficie de ataque, el tiempo de instalación, y genera confusión sobre qué sistema JWT usa el proyecto. | 🟡 MEDIA | Eliminar de `requirements.txt`. Si se planea migrar a SimpleJWT, documentarlo en el roadmap. | P3 |
| DEP-006 | `coverage` listado en `requirements.txt` de producción | `requirements.txt`: `coverage` — herramienta de análisis de cobertura que no debe ejecutarse en producción. | Añade ~500KB al contenedor de producción. Puede generar archivos `.coverage` en disco si se ejecuta accidentalmente. | 🟡 MEDIA | Crear `requirements-dev.txt` con `coverage`, `pytest`, `pytest-django` y herramientas de desarrollo. Solo `requirements.txt` se instala en el contenedor de producción. | P3 |
| DEP-007 | `numpy 1.26.4` requerido pero se instala `2.4.6` en Python 3.14 | Log de instalación: `numpy 1.26.4` no disponible para Python 3.14. pip resuelve a `numpy 2.4.6`. XGBoost 2.1.4 fue entrenado/compilado contra numpy 1.x API. | Posibles breaking changes en la API de numpy 2.x pueden causar errores silenciosos en predicciones ML o crashes en runtime al invocar XGBoost. | 🟡 MEDIA | Anclar `numpy>=1.26.4,<2.0.0` en `requirements.txt`. Usar Python 3.12 en el contenedor Docker (ya configurado) para garantizar compatibilidad. | P3 |
| DEP-008 | `scikit-learn 1.6.1` no instalable sin compilador C en Python 3.14 | Log: `ERROR: Failed building wheel for scikit-learn`. Requiere compilación de extensiones C. No hay wheel pre-compilado para Python 3.14.4 en PyPI. | Si scikit-learn se usa en el pipeline ML (p.ej., para métricas o preprocessing), el sistema ML falla completamente en entornos sin compilador. | 🟡 MEDIA | Usar el contenedor Docker con Python 3.12 donde hay wheels disponibles. Agregar al README instrucción de usar `pip install --only-binary=:all: scikit-learn`. | P3 |
| DEP-009 | Versión de Python en host (3.14.4) distinta a la requerida (3.12) en Docker | `backend/Dockerfile`: `FROM python:3.12-slim`. Host tiene Python 3.14.4. Sin gestión de versiones Python (`pyenv`, `.python-version`). | Los desarrolladores que ejecutan fuera de Docker obtienen comportamientos distintos a producción. Dependencias como `scikit-learn` fallan solo en el host pero pasan en CI. | 🟡 MEDIA | Agregar `.python-version` con `3.12.0` al repositorio. Documentar en README que el desarrollo se realiza dentro de Docker o con la versión exacta de Python. | P3 |
| CFG-003 | Sin `CONN_MAX_AGE` configurado — Django cierra conexión a PostgreSQL en cada request | `config/settings.py`: `DATABASES` sin `CONN_MAX_AGE`. Django cierra la conexión al finalizar cada request por defecto. | Overhead de TCP handshake + autenticación PostgreSQL en cada request HTTP. A 100 req/s esto genera 100 nuevas conexiones/s a PostgreSQL, agotando el límite de conexiones del servidor. | 🟡 MEDIA | Agregar `"CONN_MAX_AGE": 60` al diccionario de `DATABASES`. Para multi-worker, agregar pgBouncer. | P3 |
| CFG-004 | Sin timeout configurado en Axios para llamadas al backend | `frontend/react/src/services/api.ts`: instancia Axios sin `timeout`. Una request que no responde cuelga el hilo indefinidamente. | Si el backend queda colgado (ej: `GenerateFixturesUseCase` en transacción larga), el frontend queda congelado sin feedback al usuario. Sin timeout, el botón de "Generar Fixtures" puede parecer que funciona durante minutos. | 🟡 MEDIA | Configurar `axios.create({ timeout: 30000 })` (30 segundos). Para operaciones largas conocidas, mostrar spinner y usar polling. | P3 |
| CFG-005 | Sin `restart: unless-stopped` en servicios de `docker-compose.yml` | `docker-compose.yml`: servicios sin política de restart. Si el proceso Django falla (ej: OOM), el contenedor queda caído hasta intervención manual. | En producción, cualquier crash del backend requiere intervención manual para reiniciar el servicio. Tiempo de inactividad no acotado. | 🟡 MEDIA | Agregar `restart: unless-stopped` a los servicios `backend` y `frontend`. Para `db`, usar `restart: always`. | P3 |
| CFG-006 | Sin límites de recursos (CPU/memoria) en servicios Docker | `docker-compose.yml`: sin `deploy.resources.limits`. Un proceso ML descontrolado puede consumir toda la RAM del host y matar los demás contenedores. | Un reentrenamiento de XGBoost (`train_model.py`) o una query PostgreSQL sin LIMIT puede monopolizar recursos del host, causando OOM en los otros contenedores. | 🟡 MEDIA | Agregar en docker-compose: `deploy.resources.limits: memory: 512m, cpus: '0.5'` por servicio. Ajustar según benchmarks reales. | P3 |
| OPS-005 | Sin backup automatizado de base de datos PostgreSQL | `docker-compose.yml`: volumen `postgres_data` mapeado localmente pero sin cron de `pg_dump`, sin política de retención, sin almacenamiento offsite. | Una corrupción del volumen Docker o eliminación accidental del contenedor (`docker volume rm`) implica pérdida total de datos de torneos, equipos, y participantes. | 🟡 MEDIA | Agregar script `backup.sh` con `pg_dump` y cron diario. Para producción: configurar backup a S3 o equivalente con retención de 30 días. | P3 |
| OPS-006 | Sin API versioning implementado | `config/urls.py`: prefijo `/api/` sin versión. Todos los endpoints son `/api/recurso/` sin `/api/v1/`. | Cualquier cambio breaking en la API (nuevo campo requerido, eliminación de campo) requiere actualizar simultáneamente backend y frontend. Sin versioning, no se puede hacer deploys graduales. | 🟡 MEDIA | Agregar prefijo de versión: `path('api/v1/', include('config.urls_v1'))`. Mantener `v1` estable y deprecar explícitamente antes de introducir `v2`. | P4 |
| OPS-007 | Sin logging estructurado — logs son texto libre sin correlación | `config/settings.py`: sin configuración de `LOGGING` con formatters JSON. Los logs de Django son texto plano. Sin `request_id` que correlacione una request a través de múltiples logs. | En producción, diagnosticar un error requiere buscar en logs no estructurados. Sin request_id, es imposible correlacionar el log de autenticación con el log del use case y el error de la BD para una misma request. | 🟡 MEDIA | Configurar `LOGGING` con `python-json-logger`. Agregar middleware que genere `X-Request-ID` y lo incluya en todos los logs del request. | P3 |
| REN-003 | Sin connection pooling externo — cada worker Django abre sus propias conexiones | Sin pgBouncer ni `django-db-geventpool`. Con Gunicorn a 4 workers, se abren hasta 4 conexiones persistentes a PostgreSQL (más con `CONN_MAX_AGE=0`). | PostgreSQL tiene un límite de conexiones simultáneas (`max_connections=100` por defecto). Con múltiples workers y `CONN_MAX_AGE=0`, el límite se alcanza fácilmente bajo carga moderada. | 🟡 MEDIA | Desplegar pgBouncer en modo transaction pooling entre Gunicorn y PostgreSQL. Configurar `CONN_MAX_AGE=0` con pgBouncer (o `CONN_MAX_AGE=60` sin pgBouncer). | P3 |
| REN-004 | Modelo XGBoost cargado en `__init__` — no lazy — consumo de RAM por worker | `simulacion/infrastructure/ml/xgboost_adapter.py`: `self.model = joblib.load(...)` en el constructor. Instanciado al arranque del módulo. | Con Gunicorn a 4 workers, cada worker carga el modelo .pkl en su propio heap. Si el modelo pesa 50MB, el consumo mínimo es 200MB solo para ML, más el overhead de Django y librerías. | 🟡 MEDIA | Usar `functools.lru_cache` o singleton a nivel de módulo: `_model = None; def get_model(): global _model; if not _model: _model = joblib.load(...)`. | P3 |

---

### 🔵 SEVERIDAD BAJA

| ID | Hallazgo | Evidencia | Impacto | Severidad | Recomendación | Prioridad |
|---|---|---|---|---|---|---|
| SEG-014 | Sin política de expiración de sesión inactiva | `authentication/application/service/jwt_service.py`: refresh token válido 7 días sin consideración de inactividad. Un usuario que no usa la app 6 días puede ser reautenticado automáticamente. | Sesiones robadas por tokens de largo plazo tienen mayor ventana de oportunidad. Sin inactividad timeout, el refresh token puede ser válido aunque el usuario no haya usado la app. | 🔵 BAJA | Almacenar `last_activity` en el payload del refresh token o en Redis. Invalidar si `now - last_activity > 24h`. | P4 |
| SEG-015 | Sin headers `Content-Security-Policy` configurados | `config/settings.py`: sin configuración de CSP. `django-csp` no está instalado. | Sin CSP, el navegador no tiene restricciones sobre qué scripts puede cargar el frontend. Facilita ataques XSS de segunda fase si un atacante inyecta contenido en los datos mostrados. | 🔵 BAJA | Instalar `django-csp`. Configurar política restrictiva: `default-src 'self'; script-src 'self'`. Expandir según necesidades del frontend. | P4 |
| SEG-016 | `documento_identidad` (DNI) almacenado en texto claro en base de datos | `competencia/domain/entities/participant.py`: `documento_identidad` es un campo de texto sin cifrado. Datos de identidad de menores de edad. | En caso de breach de la base de datos, los DNI de participantes menores de edad quedan expuestos. Posible implicación de cumplimiento de normativas de protección de datos de menores (COPPA, RGPD-menores). | 🔵 BAJA | Evaluar cifrado a nivel de campo para `documento_identidad` con `django-encrypted-fields`. Mínimo: asegurar que `documento_identidad` no aparezca en logs ni en respuestas API innecesarias. | P4 |
| ARQ-012 | `competencia` sobredimensionado — 22 use cases, ~30 entidades/VOs, 27 endpoints | Análisis DDD: un solo bounded context concentra: tournament lifecycle, fixtures, qualification, inscription, standings, criteria, IA/NLP. | A medida que el sistema crece, agregar features en `competencia` requiere entender el contexto completo del módulo. Riesgo de que los cambios en fixtures afecten inadvertidamente la inscripción. | 🔵 BAJA | Planificar descomposición en sub-packages internos: `competencia/tournament/`, `competencia/fixtures/`, `competencia/qualification/`, `competencia/inscription/`. No requiere cambio de bounded context externo. | P4 |
| COD-010 | `ConfigKnockout` y `ConfigRoundRobin` tienen campos `min_teams` y `max_teams` pero sin validación cruzada al seleccionar formato | `competencia/domain/value_objects/config_tournament/`: cada config valida sus propios rangos, pero no hay validación de que el formato elegido sea compatible con el número de equipos inscritos hasta el momento de arranque. | Un manager puede configurar KNOCKOUT con `max_teams=32`, inscribir 20 equipos (no potencia de 2), y solo descubrir el error al intentar generar fixtures, no al cerrar inscripciones. | 🔵 BAJA | Agregar validación early en `TournamentState.REGISTRATION_CLOSED` que verifique compatibilidad del formato con equipos inscritos y muestre advertencia al manager. | P4 |
| DEP-010 | `openai` SDK en `requirements.txt` sin uso activo en producción | `requirements.txt`: `openai>=1.0.0,<2.0.0`. `analisis_engine.py` importa `OpenAI` pero no genera texto con LLM en código actual. | Dependencia innecesaria de 5MB+ en el contenedor. El SDK de openai puede tener vulnerabilidades propias. Genera confusión sobre capacidades del sistema. | 🔵 BAJA | Si la integración con OpenRouter no es activa, mover `openai` a `requirements-optional.txt` o `requirements-ai.txt`. Activar solo cuando la integración esté lista. | P4 |
| DEP-011 | `pip` desactualizado en imagen Docker | Log de instalación: `WARNING: pip 24.x is available (current: 23.x)`. Imagen `python:3.12-slim` incluye pip desactualizado. | pip desactualizado puede tener bugs en resolución de dependencias o vulnerabilidades conocidas. No crítico pero genera ruido en logs de build. | 🔵 BAJA | Agregar `RUN pip install --upgrade pip` en el Dockerfile antes de `pip install -r requirements.txt`. | P4 |
| DEP-012 | `django-environ` ausente — configuración de entorno gestionada manualmente | `config/settings.py`: usa `os.environ.get()` directamente sin biblioteca de gestión de configuración. Sin validación de tipos, sin valores por defecto documentados. | Los errores de configuración (ej: `DB_PORT` como string en lugar de int) solo se detectan en runtime. Sin schema de configuración, es difícil saber qué variables son obligatorias. | 🔵 BAJA | Considerar `django-environ` o `pydantic-settings` para definir schema de configuración con tipos y valores por defecto explícitos. | P4 |
| DEP-013 | Sin `dependabot` ni `renovate` para actualizaciones automáticas de dependencias | Sin `.github/dependabot.yml`. Las vulnerabilidades de dependencias (como las 8 encontradas en `npm audit`) requieren detección manual. | Las vulnerabilidades en dependencias transitivas no son detectadas hasta un `npm audit` o `pip-audit` manual. El CVE de react-router estuvo activo sin detección durante el ciclo de desarrollo. | 🔵 BAJA | Agregar `.github/dependabot.yml` con actualizaciones semanales para `pip` y `npm`. Configurar auto-merge para actualizaciones de patch. | P4 |
| CFG-007 | Sin `.env.example` completo con todos los campos y descripciones | El repositorio tiene `.env.template` o `.env.example` parcial, sin documentar campos como `LOG_LEVEL`, `XGBOOST_MODEL_PATH`, `ALLOWED_HOSTS`. | Un nuevo desarrollador o el equipo de DevOps no puede configurar el sistema correctamente sin leer el código fuente de `settings.py`. | 🔵 BAJA | Generar `.env.example` exhaustivo con todos los campos, valores de ejemplo, y comentarios indicando si son obligatorios u opcionales. | P4 |
| OPS-008 | Cobertura de tests backend desconocida — `coverage` instalado pero sin reporte | `requirements.txt` incluye `coverage` pero no hay `.coveragerc`, ni reporte en CI, ni objetivo de cobertura definido en ningún archivo de configuración. | Sin métrica de cobertura, no se puede saber qué porcentaje del dominio crítico está protegido por tests. Los 64 archivos de test podrían cubrir solo el 10% del código. | 🔵 BAJA | Configurar `.coveragerc` con `[run] source = authentication,competencia,simulacion`. Ejecutar `pytest --cov` en CI. Definir objetivo mínimo de 70% en domain layer. | P4 |
| OPS-009 | Sin CDN para assets estáticos del frontend | `frontend/react/Dockerfile`: Vite dev server sirve assets directamente. Sin Nginx, sin CDN, sin cache de browser configurado. | Los assets JavaScript/CSS se descargan desde el servidor de aplicación en cada visita de un nuevo usuario. Aumenta latencia para usuarios geográficamente distantes y carga del servidor. | 🔵 BAJA | Configurar build de producción de Vite (`vite build`) y servir desde Nginx con cabeceras `Cache-Control: max-age=31536000, immutable` para assets hasheados. | P4 |
| OPS-010 | Sin documentación de API (OpenAPI/Swagger) generada automáticamente | Ningún endpoint tiene decoradores de documentación. Sin `drf-spectacular` ni `drf-yasg` configurado. 44 endpoints sin contrato público documentado. | Los consumidores de la API (equipo frontend, integraciones futuras) deben leer el código fuente para conocer los contratos de cada endpoint. Aumenta el costo de mantenimiento y onboarding. | 🔵 BAJA | Instalar `drf-spectacular`. Agregar `@extend_schema` en las vistas clave. Exponer `/api/schema/` y `/api/docs/` (SwaggerUI). | P3 |
| OPS-011 | Sin health check endpoint en la API | Sin `GET /health/` ni `GET /api/status/`. Docker Compose sin `healthcheck` en el servicio backend. | Los orquestadores (Docker Compose, Kubernetes) no pueden determinar si el backend está listo para recibir tráfico. Los balanceadores de carga no pueden detectar instancias caídas. | 🔵 BAJA | Agregar endpoint `GET /api/health/` que retorne `200 {"status": "ok", "db": "ok"}`. Configurar `healthcheck` en docker-compose apuntando a ese endpoint. | P3 |
| OPS-012 | Sin estrategia de migración de base de datos en el proceso de deploy | `docker-compose.yml` y Dockerfile: sin comando `python manage.py migrate` automático antes de `runserver`. Las migraciones son manuales. | Un deploy de nueva versión con migraciones pendientes puede dejar la base de datos en estado inconsistente con el código en ejecución hasta que el desarrollador ejecute `migrate` manualmente. | 🔵 BAJA | Agregar en el CMD del Dockerfile o en un entrypoint script: `python manage.py migrate --noinput && python manage.py runserver ...`. | P3 |
| REN-005 | Sin índices adicionales documentados para queries frecuentes de negocio | ORM models de `competencia`: sin `class Meta: indexes = [...]` para queries frecuentes como `Match.objects.filter(torneo_id=X, estado='PENDING')` o `Team.objects.filter(torneo_id=X, estado_inscripcion='APROBADO')`. | En torneos con 64 equipos y cientos de partidos, las queries de estado sin índice hacen full table scan. La degradación es no lineal con el crecimiento de datos. | 🔵 BAJA | Ejecutar `EXPLAIN ANALYZE` en las 5 queries más frecuentes. Agregar `indexes = [Index(fields=['torneo_id', 'estado'])]` en los modelos con acceso frecuente filtrado por torneo + estado. | P4 |
| REN-006 | `GenerateFixturesUseCase` crea todos los objetos `Match` en memoria antes de persistir | `competencia/application/use_cases/generate_fixtures_use_case.py`: `_generate_knockout()` construye el árbol completo de partidos en listas Python antes de `transaction.atomic()`. | Para un torneo de 64 equipos (63 partidos), se instancian 63 objetos `Match` en memoria antes de cualquier persistencia. Si el proceso falla a mitad, se pierde todo el trabajo. Ineficiente para torneos con muchos equipos. | 🔵 BAJA | Considerar `Match.objects.bulk_create(matches)` en lugar de `save()` individual. Para torneos muy grandes, procesar en batches dentro de la transacción. | P4 |

---

## Mapa de calor de hallazgos por categoría y severidad

```
                CRÍTICA  ALTA  MEDIA  BAJA  TOTAL
SEG (Seguridad)    4      6      2      4     16
ARQ (Arquitectura) 1      3      7      1     12
COD (Código)       1      1      7      1     10
DEP (Dependencias) 2      3      5      3     13
CFG (Configuración)2      2      3      0      7
OPS (Operaciones)  0      3      5      4     12
REN (Rendimiento)  0      2      2      2      6
─────────────────────────────────────────────────
TOTAL             10     20     31     15     76
```

---

## Plan de remediación por prioridad

### P1 — Inmediato (< 48 horas) — 10 hallazgos

| ID | Acción | Responsable sugerido |
|---|---|---|
| SEG-001 | Eliminar `SECRET_KEY` fallback. Rotar si estuvo en producción. | Tech Lead |
| SEG-002 | Eliminar credenciales DB de `docker-compose.yml`. Rotar contraseña. | DevOps |
| SEG-003 | `npm install react-router@^7.16.0` | Frontend Dev |
| SEG-004 | `npm install axios@^1.8.2` | Frontend Dev |
| COD-001 | Corregir `TournamentTeam.add_participant()` — pasar `tournament_rule` como parámetro | Backend Dev |
| OPS-001 | Cambiar CMD en Dockerfile a `gunicorn` | DevOps |
| EJE-001 | Hacer lazy el import de `joblib` en `simulacion/views.py` | Backend Dev |
| EJE-002 | Hacer lazy el import de `openai` en `analisis_engine.py` | Backend Dev |
| EJE-003 | Agregar `healthcheck` y `depends_on: condition: service_healthy` en docker-compose | DevOps |
| EJE-004 | Corregir `TS18048` en `AuthContext.tsx:36`. Activar `"strict": true`. | Frontend Dev |

### P2 — Semana 1 — 22 hallazgos

Incluye: SEG-005 a SEG-010, ARQ-001, ARQ-002, ARQ-003, COD-002, DEP-001, DEP-002, DEP-003, OPS-002, OPS-003, OPS-004, CFG-001, CFG-002, REN-001, REN-002.

### P3 — Mes 1 — 30 hallazgos

Incluye: ARQ-004 a ARQ-010, COD-003 a COD-009, DEP-004 a DEP-009, CFG-003 a CFG-006, OPS-005 a OPS-007, OPS-010 a OPS-012, REN-003, REN-004.

### P4 — Backlog — 14 hallazgos

Incluye: SEG-014 a SEG-016, ARQ-011, ARQ-012, COD-010, DEP-010 a DEP-013, CFG-007, OPS-008, OPS-009, REN-005, REN-006.

---

*Matriz generada mediante síntesis de 9 auditorías: 05_Auditoria_Tecnologica · 06_Auditoria_Arquitectura · 07_Auditoria_Dependencias · 08_Guia_DevOps · 09_Auditoria_Configuracion · 10_Auditoria_Logs · 11_Inventario_Funcional · 12_Analisis_DDD · 13_Arquitectura_Empresarial · 14_Evaluacion_Calidad.*
